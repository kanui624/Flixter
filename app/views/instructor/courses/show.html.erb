<br />
<div class="booyah-box col-10 offset-1">
<br />
  <h1 class="text-center"><%= @course.title %></h1>
  <br />
  <div class="text-center">
    <%= link_to 'Add New Section', new_instructor_course_section_path(@course), class: 'btn btn-secondary btn-xs' %>
  </div>

  <br class="clr" />
  <div align="center">
    <%= link_to 'Preview As Student', course_path(@course), class: 'btn btn-secondary' %>
  </div>
<br />

    <ul>
      <% @course.sections.each do |section| %>
        <li>
          <%= section.title %>
        </li>
      <% end %>
    </ul>

</div>